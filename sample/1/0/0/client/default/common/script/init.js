
	//These codes run when the application is initialized (but not the GUI interface)
	//Do not run any code that requires the HTML nodes for the application as the application window is not created at this point

	var __common = 'sample'; //Declare application wide variable to be accessed within any classes for this application

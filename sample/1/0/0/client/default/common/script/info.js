
	//This file declares about the dependency and extra files to be loaded at start
	//Do not write any custom codes in here

	//Declare which application this application depends on
	//It is required to state one version of a system
	depend = ['system_1_0_0'];

	preload = ['resource/sample.xml']; //List any files those are not default files and need loaded to be used on the client side

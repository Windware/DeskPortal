
	//This is application styles written in JavaScript per theme

	bar = false; //To give a toolbar as default or not (Defaults to 'true')
	center = true; //To put the body content to center or not (Defaults to 'false')
	up = true; //To put the body content to align to the top or not (Defaults to 'false', will be aligned center vertically)

	window = 'cc4411'; //The window background color (Defaults to 'ffffff')
	border = '333333'; //The window border color (Defaults to '333333')

	color = '333333'; //The text color (Defaults to '333333')
	hover = '778899'; //The text color when mouse goes over it (Defaults to 'aaaaaa')

	//Where to put the window initially before the user repositions it.
	//Usually, do not specify this. It will be positioned at the center of the screen if unspecified.
	//If positioned badly, it may even get displayed outside the visible area of the browser.
	left = 15;
	top = 15;

	//The initial size of the window (Not specifying them will make it at the smallest possible size)
	width = 370;
	height = 250;

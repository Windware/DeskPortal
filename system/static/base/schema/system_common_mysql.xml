<root>
	<!-- Key/value store for each application on system database -->
	<table>
		<column name="name" param="varchar(255) UNIQUE"/>
		<column name="value" param="text"/>

		<extra name="ENGINE" param="INNODB"/>
	</table>
</root>
